export type changeHandler = (e: React.FormEvent<HTMLInputElement>) => void;

export type profExp = { company: string, jobTitle: string, location: string, description: string, startDate: string, endDate: string };