export type changeHandler = (e: React.FormEvent<HTMLInputElement>) => void;

export type profExp = {id:string, company: string, jobTitle: string, location: string, description: string, startDate: string, endDate: string };